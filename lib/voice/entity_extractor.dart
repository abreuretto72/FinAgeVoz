/// Helper to extract strongly typed entities from AI result.
class EntityExtractor {
  // Logic to parse specific fields if needed
  // e.g., standardizing date formats if AI returns varied strings
  // Currently, prompt ensures ISO 8601, but this class can validate.
}
